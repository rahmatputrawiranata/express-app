import { Request } from "express";

const insert = (a: number, b: number) => {
    return a + b;
}

const update = async(req: Request) => {
    try{



    }catch(error) {
        
    }finally{

    }
}

export {
    insert,
    update
};